const { isJust } = require("./isJust");
const { isNothing } = require("./isNothing");
const { lte, lteWith } = require("./ord");
const { of } = require("./of");

module.exports = {
  isJust,
  isNothing,
  lte,
  lteWith,
  of
};
